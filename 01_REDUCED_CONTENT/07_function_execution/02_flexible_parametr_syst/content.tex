\section{Flexible Parametrization Systems}

Some functions must accept open-ended positional values, named options, or both. Python uses variadic parameters (\texttt{*args}, \texttt{**kwargs}) and call-site unpacking (\texttt{*iterable}, \texttt{**mapping}). Default expressions are evaluated at function definition time, not on each call---the root of the mutable-default trap.
