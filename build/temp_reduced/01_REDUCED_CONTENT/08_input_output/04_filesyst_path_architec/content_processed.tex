\section{Filesystem Path Architecture}

Paths are references to filesystem locations, not file contents. Python represents them as plain strings or as \texttt{pathlib.Path} objects with structured accessors and OS delegation.
