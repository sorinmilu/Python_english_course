\section{Dictionaries (\texttt{dict}) --- Key-Value Associative Mappings}

A \texttt{dict} maps hashable keys to arbitrary values through CPython's compact open-addressed table. The sections below treat foundations, construction, internal layout, ordering semantics, and runtime mutation/query paths.
