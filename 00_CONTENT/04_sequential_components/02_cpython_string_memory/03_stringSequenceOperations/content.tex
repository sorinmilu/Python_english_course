\subsection{String Sequence Operations}
\subsubsection{Searching and Membership: `in`, `.find()`, `.index()`, `.startswith()`, and `.endswith()`}
\subsubsection{Splitting and Joining: `.split()` and `.join()` as Core Text-Sequence Transformations}
\subsubsection{Replacement and Case Transformation: `.replace()`, `.lower()`, `.upper()`, and `.casefold()`}
