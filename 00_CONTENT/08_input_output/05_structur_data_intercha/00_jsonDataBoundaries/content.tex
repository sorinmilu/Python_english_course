\subsection{JSON Data Boundaries}
\subsubsection{JSON as Text-Based Tree Serialization: Objects, Arrays, Strings, Numbers, Booleans, and Null}
\subsubsection{Loading JSON into Python Dictionaries and Lists with `json.load()` and `json.loads()`}
\subsubsection{Writing Python Structures Back to JSON with `json.dump()` and `json.dumps()`}
\subsubsection{JSON Type Mapping Boundaries: `None` vs. `null`, `dict` vs. Object, `list` vs. Array}
